export interface UserListPropType {
  sortValue: string;
  searchString: string;
}

export interface userType {
  login: string;
  id: number;
  avatar_url: string;
  html_url: string;
  repos_url: string;
}
